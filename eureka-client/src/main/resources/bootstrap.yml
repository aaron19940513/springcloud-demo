spring:
  application:
    name: eureka-client
  cloud:
    config:
      uri: http://localhost:8888
      label: master
      name: config-info
      profile: dev

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: '*'
